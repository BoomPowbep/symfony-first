{% extends "base.html.twig" %}
{% block body %}
    <p>coucou 🛡 💽 🚝 🍺</p>
    <p>{{ toto }}</p>
    {% for post in posts %}
        <h1>{{ post.title }}</h1>
        <p>{{ post.author }}</p>
        <p>{{ post.content }}</p>
        <hr>
    {% endfor %}
{% endblock %}
