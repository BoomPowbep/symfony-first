{% extends "base.html.twig" %}
{% block body %}
    <p>coucou 🛡 💽 🚝 🍺</p>
    <p>{{ toto }}</p>
    <h1>{{ post.title }}</h1>
    <p>{{ post.author }}</p>
    <p>{{ post.content }}</p>
{% endblock %}
