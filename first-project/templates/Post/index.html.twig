{% extends "base.html.twig" %}
{% block body %}
    <a href="{{ path('add') }}">Add</a>
    <p>coucou 🛡 💽 🚝 🍺</p>
    <p>{{ toto }}</p>
    {% for post in posts %}
        <a href="{{ path('view', { 'id' : post.id }) }}"><h1>{{ post.title }}</h1></a>
        <p>{{ post.author }}</p>
        <p>{{ post.content }}</p>
        <hr>
    {% endfor %}
{% endblock %}
